% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spill.R
\name{plot_spill}
\alias{plot_spill}
\title{Plot per-interaction spillover scatter panels}
\usage{
plot_spill(
  spill_matrix,
  cytof_data,
  panel_table,
  n_cells = 1000,
  my_seed = 1234,
  exclude_channels = NULL,
  n_col = 6,
  mode = c("spill_matrix", "valid_interactions"),
  valid_interactions = c("same", "ox", "one_plus", "one_minus"),
  rasterize = TRUE,
  dpi = 150,
  title_size = 8,
  axis_size = 7,
  organize_by = c("source", "destination")
)
}
\arguments{
\item{spill_matrix}{Numeric spillover matrix, channel names on both dims
(rows = source, cols = destination).}

\item{cytof_data}{A \code{SingleCellExperiment} (CATALYST-style) with an \code{exprs}
assay and a \code{sample_id} colData column; antigens as rownames.}

\item{panel_table}{Data frame mapping channels to antigens via \code{fcs_colname}
and \code{antigen} columns.}

\item{n_cells}{Cells to sub-sample per sample for the scatters, Default: 1000.}

\item{my_seed}{RNG seed for the sub-sample, Default: 1234.}

\item{exclude_channels}{Optional character vector of source channels to skip.}

\item{n_col}{Columns in the final panel grid, Default: 6.}

\item{mode}{One of \code{"spill_matrix"} / \code{"valid_interactions"}, Default: "spill_matrix".}

\item{valid_interactions}{Which interaction types count as valid in
\code{"valid_interactions"} mode, Default: c("same","ox","one_plus","one_minus").}

\item{rasterize, dpi}{Retained for back-compatibility; rasterization is a no-op
here (the package is Cairo-/ggrastr-free — hexbin panels render fine as vector).}

\item{title_size, axis_size}{Panel title / axis text sizes, Default: 8 / 7.}

\item{organize_by}{Group + order panels by \code{"source"} or \code{"destination"},
Default: "source".}
}
\value{
A composed grid (\code{cowplot::plot_grid}) of scatter panels, or \code{NULL} if
there is nothing to plot.
}
\description{
Build a grid of biaxial scatter panels for the channel pairs in a
spillover matrix, to eyeball compensation. Supports two modes: \code{"spill_matrix"}
(every non-zero entry) and \code{"valid_interactions"} (only physically plausible
interactions: same element, +16 oxide, +/-1 isotope), and groups/orders panels
by source or destination channel. Panels are titled with the spillover
coefficient and the source/dest antigen names from \code{panel_table}.
}
\note{
Relies on a \code{plotScatter()} (the CATALYST biaxial plotter) being available
in scope, as the analysis workflow attaches it.
}
