% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compCytof2.R
\name{compCytof2}
\alias{compCytof2}
\title{Compensate a SingleCellExperiment (CATALYST-free), with a progress bar}
\usage{
compCytof2(
  x,
  sm = NULL,
  method = c("nnls", "flow"),
  assay = "counts",
  overwrite = TRUE,
  transform = TRUE,
  cofactor = NULL,
  verbose = TRUE,
  cl = NULL
)
}
\arguments{
\item{x}{A \code{SingleCellExperiment} (rows = channels; \code{rowData(x)$channel_name} set, as
produced by \code{prepData2} / \code{CATALYST::prepData}).}

\item{sm}{Spillover matrix (rows = emitting, cols = receiving channels), named by
channel on both dims. If \code{NULL}, taken from \code{metadata(x)$spillover_matrix}.}

\item{method}{\code{"nnls"} (per-cell non-negative least squares; default) or \code{"flow"}
(vectorised linear unmixing via \code{flowCore::compensate}).}

\item{assay}{Assay to compensate, Default: 'counts'.}

\item{overwrite}{Overwrite \code{assay} (and \code{exprs}) in place; else write to
\code{compcounts} / \code{compexprs}, Default: TRUE.}

\item{transform}{Add an arcsinh assay computed from the compensated counts,
Default: TRUE.}

\item{cofactor}{arcsinh cofactor; if \code{NULL}, taken from \code{int_metadata(x)$cofactor},
Default: NULL.}

\item{verbose}{Show a progress bar on the NNLS solve, Default: TRUE.}

\item{cl}{Passed to \code{pbapply::pbapply} for parallelism (integer cores or a cluster);
\code{NULL} = serial. Only used when \code{verbose = TRUE}, Default: NULL.}
}
\value{
The \code{SingleCellExperiment} with compensated assay(s); cell order preserved.
}
\description{
Clean-room, CATALYST-free re-implementation of \code{CATALYST::compCytof()}
for mass-cytometry compensation. Applies a spillover matrix \code{sm} to an assay
(default \code{counts}), writes the compensated values back (overwriting the assay, or
into \code{compcounts}), and optionally adds the arcsinh \code{exprs}. The per-cell NNLS solve
— the slow part on big (e.g. un-debarcoded, concatenated) files — shows a
Seurat-style progress bar via \code{pbapply}.

Differences vs CATALYST: the spillover matrix is aligned to the data channels by
\strong{name only} (no isotope-impurity inference). This is correct when \code{sm} already
covers the channels you compensate — e.g. a \code{computeSpillmat} / \code{update_spill_matrix2}
matrix over the panel + barcode channels. Channels present in the data but not in
\code{sm} are left uncompensated (identity diagonal); channels in \code{sm} but not the data
are dropped. Because \code{sm} is expanded to the full data-channel set (identity for
unknowns), there is no dimension mismatch even when the SCE still carries non-mass
channels (Time, Gaussian) — they pass through untouched.

\code{cofactor} is treated as a single scalar (the common case); per-channel cofactors
are not reproduced.
}
