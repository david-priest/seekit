% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepData2.R
\name{prepData2}
\alias{prepData2}
\title{Build a SingleCellExperiment from FCS data (CATALYST-free), with Time/channel options}
\usage{
prepData2(
  x,
  panel = NULL,
  md = NULL,
  features = NULL,
  transform = TRUE,
  cofactor = 5,
  panel_cols = list(channel = "fcs_colname", antigen = "antigen", class = "marker_class"),
  md_cols = list(file = "file_name", id = "sample_id", factors = c("condition",
    "patient_id")),
  by_time = TRUE,
  FACS = FALSE,
  keep_time = FALSE,
  time_col = "Time",
  keep_channels = NULL,
  channel_names = c("antigen", "fcs", "desc"),
  ...
)
}
\arguments{
\item{x}{A \code{flowSet}, \code{flowFrame}, list of \code{flowFrame}s, or a character path to a
directory of FCS files / vector of FCS paths.}

\item{panel, md}{Panel and metadata \code{data.frame}s (required).}

\item{features}{Channels to keep (default: the panel channels). \code{keep_time} /
\code{keep_channels} also retain their channels regardless.}

\item{transform}{If \code{TRUE}, add an \code{exprs} assay: \code{arcsinh(x/cofactor)} for all
channels except the acquisition-level raw params (Time, Event_length, cell_length,
file_number), which stay linear. Default: TRUE.}

\item{cofactor}{arcsinh cofactor (mass channels only), Default: 5.}

\item{panel_cols, md_cols}{Column-name maps, as in \code{CATALYST::prepData}.}

\item{by_time}{Order samples by \verb{$BTIM} when \code{md} is \code{NULL}, Default: TRUE.}

\item{FACS}{If \code{TRUE}, keep all channels as rows (no mass-channel subset), Default: FALSE.}

\item{keep_time}{Attach the raw acquisition Time to \code{colData}, Default: FALSE.}

\item{time_col}{Name of the new colData Time column, Default: 'Time'.}

\item{keep_channels}{Non-panel channels to retain as assay rows (\verb{marker_class "none"}, raw \code{exprs}): a character vector of channel names, or \code{"all"} for every
non-mass channel. Default: NULL.}

\item{channel_names}{What to name the SCE rows (channels):
\itemize{
\item \code{"antigen"} (default) — rename panel channels to their antigen (\code{CD32}, …); the
usual analysis form.
\item \code{"fcs"} — keep the raw FCS channel identifiers (\verb{$PnN}: \code{Cd106Di}, \code{In115Di}, …).
Use this for a pre-processing / debarcoding SCE (e.g. for GateLabR): the exported
FCS keeps \verb{$PnN}, so a later "real" import with a \verb{$PnN}-keyed \code{panel} renames to
antigens cleanly. No panel-based renaming is applied (marker_class still is).
\item \code{"desc"} — keep the raw FCS marker labels (\verb{$PnS}: \verb{106Cd_CD32}, \verb{115In_CD45}, …;
what GateLabR shows), falling back to \verb{$PnN} where the desc is blank.
}}

\item{...}{Passed to \code{read.FCS} when \code{x} is a path (e.g. \code{truncate_max_range}).}
}
\value{
A \code{SingleCellExperiment}. \code{metadata()} also carries \code{pnn_to_channel} /
\code{channel_to_pnn} (\verb{$PnN} <-> marker/display-name maps) so gating apps (e.g.
GateLabR) can resolve gating-ML metal-channel references and restore \verb{$PnN} on
FCS export.
}
\description{
Clean-room, CATALYST-free re-implementation of \code{CATALYST::prepData()}
for the common mass-cytometry case (a \code{flowSet} or FCS directory + a \code{panel} and
\code{md}). It builds the \code{counts} (and \code{exprs}) assays, marker-class \code{rowData},
per-cell \code{colData} (sample_id + factors), and \code{experiment_info} metadata. By
default non-mass channels (Time, Event_length, gaussian params, …) are moved into
\code{int_colData} and the rows subset to mass channels — exactly like \code{prepData}.

Two additions over CATALYST:
\itemize{
\item \strong{\code{keep_time}} surfaces the acquisition \code{Time} channel into the user-facing
\code{colData} (column \code{time_col}), so you can colour/gate by it directly. Time is
taken \strong{raw} per event (CATALYST's cumulative cross-sample offset is omitted —
it assumes sequential acquisitions and is wrong for debarcoded single-run data).
\item \strong{\code{keep_channels}} retains chosen non-panel channels (Time, Event_length, the
gaussian params, …) as \strong{assay rows} (\code{marker_class = "none"}) instead of
discarding them to \code{int_colData} — e.g. so a downstream gating app can gate on
them. \code{keep_channels = "all"} keeps every non-mass channel. In \code{exprs}, kept
gaussian params (Center/Width/Residual/Offset/Amplitude) are arcsinh-transformed
like the metals (matching Cytobank / GateLabR gating space); only Time,
Event_length, cell_length and file_number stay linear.
}

Simplifications vs CATALYST: \code{panel} and \code{md} are required (no \code{guessPanel}), and
the multi-FCS channel harmonisation is not reproduced.
}
